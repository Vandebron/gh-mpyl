---
$schema: https://json-schema.org/draft/2020-12/schema#
$id: mpyl_stages.schema.yml
definitions:
  stageNames:
    enum:
      - build
      - test
      - deploy
      - postdeploy
  dependencies:
    type: array
    minItems: 1
