name: 'nodeservice'
description: 'This is a test container. For testing the MPyL pipelines, not to be deployed anywhere.'
projectYmlVersion: 3
stages:
  build: ''
  deploy: Echo Deploy
maintainer: ['MPyL']
dependencies:
  build:
    - 'test/docker/'
